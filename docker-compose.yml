version: '3.5'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: queens-game
    ports:
      - '${PORT}:${PORT}'
    volumes:
      - .:/app
      - /app/node_modules
    tty: true
version: '3'
name: queens-game
services:
  server:
    container_name: queens-game
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - '${PORT}:${PORT}'
    expose:
      - '${PORT}'
    tty: true
    stdin_open: true